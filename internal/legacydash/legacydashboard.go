package legacydash

// In theory, this Dashboard should match the expected incoming (legacy) schema,
// so we don't need to fight with simplejson and paths while migrating to the
// latest schema. This may require multiple types.
type Dashboard struct{}
